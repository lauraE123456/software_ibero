{
	"scripts": {
		"dev": "concurrently --names \"SERVER,CLIENT\" --prefix \"[{name}]\" \"pnpm run server\" \"pnpm run client\"",
		"server": "cd server && pnpm run dev",
		"client": "cd client && pnpm run dev"
	},
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}